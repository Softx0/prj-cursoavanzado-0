<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/white"
    android:elevation="10dp"
    android:minHeight="?attr/actionBarSize"
    android:gravity="center">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ic_upload"
            android:src="@drawable/ic_backup_black_24dp"
            style="@style/style_image_upload" />

        <ImageView
            android:id="@+id/ic_iconActionBar"
            android:src="@mipmap/logo_action_bar"
            style="@style/style_logo_mpv_action_bar_post" />

        <ImageView
            android:id="@+id/sign_out"
            android:src="@mipmap/sign_out"
            style="@style/style_action_bar_sign_out" />

    </LinearLayout>

</android.support.v7.widget.Toolbar>